[project]
name = "nature-music-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { email = "natrajexplore@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.141.1",
    "numpy>=2.4.6",
    "scipy>=1.17.1",
    "uvicorn>=0.53.0",
]

[project.scripts]
nature-music-app = "nature_music_app:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
